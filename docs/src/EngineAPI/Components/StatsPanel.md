### StatsPanel

Shows a three.js performance monitor (FPS and frame time) in the runtime UI. Handy to keep an eye on performance while you playtest.

Add the component to any object in the scene. It has no props. It appends its panel to the runtime UI when the scene plays and updates it every frame.
